node_modules/
dist/
build/
.next/
out/
coverage/

.env
.env.local
*.local

*.log
npm-debug.log*

.DS_Store
Thumbs.db

*.tsbuildinfo

# Prisma
product/api/prisma/migrations/dev.db
# provider/api's Prisma client has a custom output path (a separate
# workspace's generated client can't share product/api's node_modules
# location — see schema.prisma's header comment) — regenerated via
# `prisma generate`, never hand-edited, never committed.
provider/api/src/generated/

# Uploaded documents (local disk storage in dev)
product/api/storage/

# Integration test shared session (written by globalSetup, read by test files)
product/api/tests/integration/.session.json

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
