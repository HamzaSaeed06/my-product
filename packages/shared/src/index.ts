// Empty on purpose. Shared types/Zod schemas/utilities move here once two or
// more apps (product/api, product/web, and eventually platform/*) actually
// need to share them — don't pre-populate this speculatively.
export {};
